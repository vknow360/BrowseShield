{
  "name": "extension",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:firefox": "vite build --mode firefox",
    "build:onnx-stripped": "echo 'This would trigger the custom emscripten build of ONNX Runtime with WebGPU only (saves 15MB)'",
    "lint": "web-ext lint -s dist"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.1.43",
    "vite": "^8.0.0",
    "vite-plugin-zip-pack": "^1.2.4",
    "web-ext": "^10.6.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1",
    "@xenova/transformers": "^2.17.2",
    "onnxruntime-web": "^1.29.0",
    "tesseract.js": "^7.0.0",
    "webextension-polyfill": "^0.12.0"
  }
}
